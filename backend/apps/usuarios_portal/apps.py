from django.apps import AppConfig

class UsuariosPortalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.usuarios_portal'
    verbose_name = 'Usuarios Portal'
